{% extends 'UIBundle::base.html.twig' %}

{% block body %}
{% for question in questions %}
<div>
    <h4>{{ question.text }}</h4>
    <p>{{ question.endDate|date('Y-m-d H:i:s') }}</p>
</div>
{% endfor %}

{% endblock %}
